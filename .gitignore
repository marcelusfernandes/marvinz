# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env

node_modules
dist
dist-ssr
dist-electron
release
*.local 

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local working docs (specs, issues, reports — not for repo)
.docs/

# Claude Code agent worktrees (runtime artifacts)
.claude/worktrees/

# Marvin snapshot storage (runtime artifacts, not for repo)
**/.marvin/

# Test output (runtime artifacts, not for repo)
coverage/
test-results/
playwright-report/
.marvin/
.claude/scheduled_tasks.lock

# Generated project-mapping docs (droid wiki) — not versioned for now
droid-wiki/

# Harness outcome ledger lives on the orphan `complexity-ledger` branch, not here.
# trend-report materializes it locally via `git show`; never (re)commit it to develop.
_complexity-ledger/outcomes.jsonl
